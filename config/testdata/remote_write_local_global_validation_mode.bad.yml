global:
  metric_name_validation_scheme: utf8
remote_write:
  - url: localhost:9201
    metric_name_validation_scheme: legacy
    write_relabel_configs:
      - source_labels: [ "test" ]
        regex: expensive.*
        action: drop
        target_label: "test("
