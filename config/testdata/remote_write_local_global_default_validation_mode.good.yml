global:
  metric_name_validation_scheme: legacy
remote_write:
  - url: localhost:9201
    write_relabel_configs:
      - source_labels: [ "test" ]
        regex: expensive.*
        action: drop
        target_label: "test("
